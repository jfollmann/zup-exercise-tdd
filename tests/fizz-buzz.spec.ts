const fizzBuss = (): any => {
  return []
}

describe('Fizz Buss Spec', () => {
  test('Call function with a correct params', () => {
    const result = fizzBuss()

    expect(result).toEqual([])
  })
})
